import "package:flutter/material.dart";

class CtrlStreamingControls extends StatelessWidget{
  const CtrlStreamingControls({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
}